import ContainerRoot from "./container-root.svelte";
import ContainerBody from "./container-body.svelte";
import ContainerTitle from "./container-title.svelte";

const Container = {
  Root: ContainerRoot,
  Body: ContainerBody,
  Title: ContainerTitle,
};

export default Container;
